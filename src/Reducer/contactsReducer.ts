import { contacts } from "../static-data"

// I changed it from this to the new one. Uncommnent if it doesnt work later
// export default (state = contacts, action) => {
//     return state;
// }
export default function contact(state = contacts, action:any) {
    return state;
}
